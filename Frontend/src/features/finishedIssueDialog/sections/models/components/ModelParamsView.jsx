import { IssueModelParametersView } from "../../../../modelParameters";

const ModelParamsView = ({ parameters, values, parameterContext }) => <IssueModelParametersView parameters={parameters} values={values} parameterContext={parameterContext} />;

export default ModelParamsView;
