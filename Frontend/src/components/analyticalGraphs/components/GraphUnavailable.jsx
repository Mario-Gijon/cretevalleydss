import { Typography } from "@mui/material";

const GraphUnavailable = () => <Typography variant="body2" color="text.secondary" sx={{ minHeight: 96, display: "grid", placeItems: "center" }}>This graph is unavailable.</Typography>;

export default GraphUnavailable;
