// Generated by ModelForge.
//
// Implementation guide
// Purpose:
// - Validate and normalize the definition used when creating an ExpressionDomain
//   of type "{{ type_key }}".
//
// Receives:
// - definition: JSON submitted by the frontend CreationForm for this type.
//
// Must return:
// - The normalized definition object that will be stored in ExpressionDomain.definition.
//
// ModelForge guidance:
// - definitionExample is provided only as implementation guidance.
// - constraintExample is the API/registry field name used by model scaffolds as a
//   constraint template. It is metadata guidance, not runtime validation by itself.
//
// Keep unchanged:
// - Until this file is implemented, keep the safe under-development error contract:
//   code: "EXPRESSION_DOMAIN_TYPE_UNDER_DEVELOPMENT"
//   field: "definition"
//
// Safe to edit:
// - Replace the placeholder error with real validation and normalization logic.
// - Keep the exported function name and return contract stable.
//
{{ definition_example_comment_block }}

import { createBadRequestError } from "../../../../../utils/common/errors.js";

export const {{ backend_creation_definition_function_name }} = (
  definition
) => {
  void definition;

  throw createBadRequestError(
    "{{ type_key }} is a generated scaffold. Implement creation.js before using this type.",
    {
      code: "EXPRESSION_DOMAIN_TYPE_UNDER_DEVELOPMENT",
      field: "definition",
      details: {
        typeKey: "{{ type_key }}",
      },
    }
  );
};
