// Generated by ModelForge.
//
// Implementation guide
// Purpose:
// - Collect one expert evaluation value for a selected "{{ type_key }}" expression domain.
//
// Receives:
// - value: the current evaluation value.
// - onChange(nextValue): call this whenever the evaluation value changes.
// - disabled: disable interactive controls when true.
// - error: whether the current value should be shown as invalid.
// - helperText: contextual validation/help text from the caller.
//
// Emits:
// - onChange(nextValue) with a value accepted by backend "./evaluation.js".
//
// Must match:
// - The emitted value must match what backend validateEvaluation({ value, expressionDomain })
//   normalizes and accepts.
//
// ModelForge guidance:
// - evaluationExample is included below only as implementation guidance.
//
// Safe to edit:
// - Replace the placeholder alert with the real Material UI input controls for this type.
// - Keep the prop contract and onChange(nextValue) behavior stable.
//
// Material UI reference:
// - https://mui.com/material-ui/
// - This template currently uses: Alert, Stack
//
{{ evaluation_example_comment_block }}

import { Alert, Stack } from "@mui/material";

export const {{ evaluation_input_component_name }} = ({
  value,
  onChange,
  disabled = false,
  error = false,
  helperText = "",
}) => {
  void value;
  void onChange;
  void disabled;

  return (
    <Stack spacing={1}>
      <Alert severity={error ? "error" : "info"} variant="outlined">
        {helperText || "Generated scaffold. Implement the domain-specific evaluation input here."}
      </Alert>
    </Stack>
  );
};

export default {{ evaluation_input_component_name }};
