// Generated by ModelForge.

import { Alert } from "@mui/material";

const {{ view_component_name }} = ({
  decisionContext,
  evaluation,
  setEvaluation,
  collectiveEvaluation,
  readOnly,
  loading,
}) => {
  // decisionContext contains the problem data available to this structure.

  // Uncomment to inspect the real context received by the view:
  // console.log(JSON.stringify(decisionContext, null, 2));

  // evaluation is the complete current payload.

  // To replace the complete payload:
  // const nextEvaluation = structuredClone(evaluation ?? {});
  // Modify nextEvaluation according to this structure.
  // setEvaluation(nextEvaluation);

  void decisionContext;
  void evaluation;
  void setEvaluation;
  void collectiveEvaluation;
  void readOnly;
  void loading;

  return (
    <Alert severity="info">
      {{ evaluation_structure_key }} is under development.
    </Alert>
  );
};

export default {{ view_component_name }};
