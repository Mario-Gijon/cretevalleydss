// Generated by ModelForge.
// TODO: implement this structure's criterion-reference remapping.
//
// This operation runs during creator-side API criteria weighting after criteria
// have been persisted. Replace temporary criterion IDs inside the plugin-owned
// payload with the persisted criterion IDs from criterionIdMap.
//
// ModelForge does not guess where criterion IDs appear in the payload. Do not
// implement generic recursive string replacement. Return payload unchanged only
// after explicitly confirming that this payload contains no criterion references.

export const {{ remap_criterion_ids_function_name }} = ({
  payload,
  criterionIdMap,
}) => {
  void payload;
  void criterionIdMap;

  throw new Error(
    "{{ evaluation_structure_key }} remapCriterionIds must be implemented before creator-side use. If this payload contains no criterion IDs, explicitly return payload unchanged."
  );
};
