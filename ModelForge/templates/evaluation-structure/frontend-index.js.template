// Generated by ModelForge.
// Implementation guide
// Purpose: register the frontend View component for this evaluation structure.
// Keep unchanged: key and stage must stay aligned with the generated backend
// plugin so the frontend registry resolves the correct View at runtime.
// Change implementationStatus to "ready" only after the structure is complete
// and tested.

import { EVALUATION_STAGES } from "../../evaluationStages";
import {{ view_component_name }} from "./{{ view_component_name }}";
{{ build_initial_evaluation_import }}

export const {{ evaluation_structure_key }}Structure = Object.freeze({
  key: "{{ evaluation_structure_key }}",
  stage: EVALUATION_STAGES.{{ stage_constant }},
  implementationStatus: "scaffold",
  View: {{ view_component_name }},
{{ build_initial_evaluation_registry_field }}
});
