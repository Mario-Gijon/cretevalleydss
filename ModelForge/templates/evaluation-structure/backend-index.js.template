// Generated by ModelForge.

import {
  EVALUATION_STAGES,
} from "../../evaluationStages.js";

export const {{ backend_structure_export_name }} = Object.freeze({
  key: "{{ evaluation_structure_key }}",
  stage: EVALUATION_STAGES.{{ stage_constant }},
  implementationStatus: "scaffold",

  async get({ payload, decisionContext }) {
    void decisionContext;

    // Return the complete payload that must be sent to the Frontend.
    // To send the stored payload without changes:
    // return payload;
    return payload ?? {};
  },

  async save({ payload, decisionContext, mode }) {
    void decisionContext;
    void mode;

    // mode === "draft" normally allows incomplete data.
    // mode === "submit" is the definitive submission.
    // Return the complete payload that must be stored in the database.
    // To store the received payload without changes:
    // return payload;
    return payload;
  },
});
