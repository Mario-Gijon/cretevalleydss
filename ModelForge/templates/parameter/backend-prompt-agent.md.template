# Implement {{ parameter_structure_key }} Backend — Agent prompt

Read generated Parameter Structure files, `IMPLEMENTATION_GUIDE.md`, matching
Frontend when useful, and:

```text
Backend/utils/common/errors.js
Backend/utils/common/objects.js
Backend/utils/common/strings.js
```

## Developer requirements

### Parameter Structure description
[PARAMETER STRUCTURE DESCRIPTION]

### Canonical value shape
[VALUE SHAPE]

### Default and restrictions semantics
[DEFAULT / RESTRICTIONS CONTRACT]

### Additional requirements
[ADDITIONAL REQUIREMENTS]

### Actual runtime input — optional
[ACTUAL RUNTIME INPUT]

Preserve:

```js
{{ validate_function_name }}({ value, parameter, context })
```

Ordinary invalid user values return `{ ok: false, message, value }`; they are not
exceptions. Valid values return `{ ok: true, value: normalizedValue }`.

Use `createInternalError(...)` only for genuine malformed/inconsistent
application runtime context. Reuse existing object/string helpers.

Do not duplicate higher-level required/default/key orchestration.

Only use Expression Domain validation when this parameter actually stores a
criterion-domain value.

Keep code small, immutable and local. No generic one-use abstractions or
compatibility fallbacks.

Change `"scaffold"` to `"ready"` when requested runtime behavior is complete.

Tests are outside scope. Do not create/modify/run tests unless explicitly
requested.

Run targeted lint/static validation when practical plus:

```text
git diff --check
```

Do not install dependencies.

Report files changed, canonical semantics, defaults/restrictions, context used,
shared helpers/errors, final status and validation.
