# Implement {{ display_name }} integration — Agent prompt

Implement only the non-mathematical integration in:

```text
DecisionModelsService/models/{{ snake_case_model_name }}/
```

Do NOT implement/invent `run.py` math.

Read the generated package, referenced Evaluation/Parameter Structures when
relevant, and:

```text
DecisionModelsService/services/model_executors/responses.py
DecisionModelsService/schemas/model_requests.py
```

Inspect the closest relevant executor for conventions.

## Developer requirements

### Model input semantics
[MODEL INPUT REQUIREMENTS]

### Evaluation payload semantics
[EVALUATION PAYLOAD SEMANTICS]

### Expected public output
[EXPECTED PUBLIC OUTPUT]

### Additional integration requirements
[ADDITIONAL REQUIREMENTS]

### Actual runtime input — optional
[ACTUAL RUNTIME INPUT]

Preserve generated metadata.

Use `GenericModelExecutionRequest` and existing `success_response` /
`error_response`; do not invent another response envelope.

Implement only request validation/normalization, ID/order resolution, declared
parameter/domain interpretation, preparation of later algorithm input and safe
known output mapping.

Do not move algorithm logic into `executor.py`.

Keep a safe under-development response when the algorithm is required for a
meaningful success result.

Keep `implementation_status="scaffold"` because `run.py` remains unimplemented.

Tests are outside scope. Do not create/modify/run tests unless explicitly
requested.

Run appropriate Python syntax/static/lint checks when practical plus:

```text
git diff --check
```

Do not install dependencies.

Report files changed, request normalization, contracts consumed, response helper
reuse, confirmation that algorithm was not implemented, final status and
validation.
